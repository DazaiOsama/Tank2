#include "loli.h"
#include <iostream>
#include <cstdlib>
#include <cstring>
using namespace std;


/*loler::loler(){
    strcpy(m_filename,"temp.txt");
    cout<<"constructor"<<endl;
       }
void loler::setFileName()
{
cout<<"Enter file name"<<endl;
cin>>m_filename;
cin.get();
}
void loler::setData()
{
  cout<<"Enter text"<<endl;
  cin.getline(m_data,HP);
}
void loler::write()
{
    FILE*f;
    f=fopen(m_filename,"wt");
    int realLen = strlen(m_data);
    fwrite(m_data,sizeof(char),realLen,f);
    fclose(f);
}
void loler::readFromFile()
{
    FILE*f;
    f=fopen(m_filename,"rt");
    strcpy(m_data, "");
    memset(m_data, '\0',HP);
    fread(m_data,sizeof(char),HP,f);
    fclose(f);
    cout<<"DADDY from file \n";
    cout<<m_data<<endl;
}
*/
